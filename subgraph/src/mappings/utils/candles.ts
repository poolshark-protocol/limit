import { LimitPool } from "../../../generated/schema";


export function updatePoolHourData(pool: LimitPool): LimitPool {
    // check length of array
    if (pool.last24HoursPoolData.length == 0) {
      
    }
    return pool
}